# Backblaze B2 API credentials
B2_APPLICATION_KEY_ID=your_key_id_here
B2_APPLICATION_KEY=your_application_key_here

# Snapshot settings
SNAPSHOT_SCHEDULE_TYPE=interval
SNAPSHOT_INTERVAL_HOURS=24
SNAPSHOT_HOUR=0
SNAPSHOT_MINUTE=0
SNAPSHOT_DAY_OF_WEEK=0
SNAPSHOT_DAY_OF_MONTH=1
SNAPSHOT_RETAIN_DAYS=90
COST_CHANGE_THRESHOLD=10.0

# Web application settings
SECRET_KEY=change_this_to_a_random_string
DEBUG=False
HOST=0.0.0.0
PORT=5000

# Email notification settings
EMAIL_ENABLED=False
EMAIL_SENDER=your_email@example.com
EMAIL_RECIPIENTS=recipient1@example.com,recipient2@example.com
EMAIL_SERVER=smtp.example.com
EMAIL_PORT=587
EMAIL_USERNAME=your_username
EMAIL_PASSWORD=your_password
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False

# Backblaze pricing settings (optional, default values shown)
# STORAGE_COST_PER_GB=0.005
# DOWNLOAD_COST_PER_GB=0.01
# CLASS_A_TRANSACTION_COST=0.004
# CLASS_B_TRANSACTION_COST=0.0004

# API efficiency settings (optional, default values shown)
# API_CACHE_TTL=3600
# BUCKET_STATS_CACHE_HOURS=24
# MAX_FILES_PER_BUCKET=10000
# USE_ACCURATE_BUCKET_SIZE=True  # Set to False for faster but less accurate bucket size calculation
# USE_S3_API=True                # Set to True to use S3 API for most accurate bucket size reporting

# Cloudflared settings (optional)
# If provided, cloudflared will be used as a proxy to expose your app to the internet securely
# Leave empty to disable cloudflared and use direct access
# CLOUDFLARE_TUNNEL_TOKEN=your_cloudflare_tunnel_token
